# Tile: Grundwasser Situation with state-based colors. Requires card-mod (HACS).
# Entity: stable alias (integration/grundwasser_helpers.yaml) — not the REST source (may have _2, _3 suffix).
# States: kein Niedrigwasser → green, niedrig → amber, Niedrigwasser → red.
---
type: tile
entity: sensor.grundwasser_poing_d83_situation_anzeige
grid_options:
  columns: 12
  rows: 1
icon: mdi:water
card_mod:
  style:
    ha-card: |
      {% set s = states('sensor.grundwasser_poing_d83_situation_anzeige') %}
      {% if s == 'kein Niedrigwasser' %}
      --tile-color: #4caf50 !important;
      {% elif s == 'niedrig' %}
      --tile-color: #ff9800 !important;
      {% elif s == 'Niedrigwasser' %}
      --tile-color: #f44336 !important;
      {% else %}
      --tile-color: var(--secondary-text-color) !important;
      {% endif %}
    .icon-container .icon$: |
      .shape {
        {% set s = states('sensor.grundwasser_poing_d83_situation_anzeige') %}
        {% if s == 'kein Niedrigwasser' %}
        background: #4caf50 !important;
        {% elif s == 'niedrig' %}
        background: #ff9800 !important;
        {% elif s == 'Niedrigwasser' %}
        background: #f44336 !important;
        {% else %}
        background: var(--secondary-text-color) !important;
        {% endif %}
      }
